import { db, tableNames } from './db.js'
import { queryRowsToArray, buildSelectString, buildInsertString } from './dbutils.js'

// TODO: get name
// getUserName(user_id, user_name)

// TODO: create/update name
// createUserName(user_id, user_name)


// TODO: get bio
// getUserBio(user_id, bio)

// TODO: create/update bio
// createUserBio(user_id, bio)